export interface User {
    id?: number,
    name?: string,
    birthdate?: string,
    email?: string,
    password?: string,
    rol?: number,
    createdDate?: string,
    image?: string,
    eliminado?: number,
    auth?: number,
    auth_code?: number,
    code_try?: number
}